import React from 'react';export const Header = (props) => {	const back = <span className={'icon icon-back'}></span>;	const title = <p className={'title'}>{props.title}</p>;	const more = <span className={'icon icon-more'}></span>;	return (		<div className="topBar" style={styles.topBar}>			{props.back ? props.back : back}			{props.content ? props.content : title}			{props.more ? props.more : more}		</div>	)};const styles = {	topBar: {		height: '100px',		padding: '0 20px',	},}